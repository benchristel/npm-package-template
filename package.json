{
  "name": "npm-package-template",
  "version": "1.0.0",
  "description": "Template app",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-inject": "^4.0.4",
    "husky": "^8.0.1",
    "prettier": "^2.7.1",
    "rollup": "^2.0.0",
    "typescript": "^4.8.4",
    "vite": "^5.1.1"
  },
  "dependencies": {
    "@benchristel/taste": "0.5.0"
  },
  "scripts": {
    "build": "rm -rf dist && yarn tsc --project tsconfig.build.json --outDir dist",
    "dev": "yarn vite ./test-runner --config vite.config.js",
    "format": "node_modules/.bin/prettier --write .",
    "setup": "yarn install; yarn run husky install",
    "smoke": "yarn vite preview --open",
    "ts": "yarn tsc --watch --noEmit"
  }
}
